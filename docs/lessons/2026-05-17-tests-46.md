# 테스트 품질 수정 회고: #46

날짜: 2026-05-17

## Issue 1 — assertThrows 금지 패턴 교체

**문제**: `AhoCorasickAutomatonTest`, `AhoCorasickDslTest`에 `org.junit.jupiter.api.assertThrows` 사용.
CLAUDE.md: "Do not use JUnit assertThrows in new tests."

**수정**: `io.bluetape4k.assertions.assertFailsWith<T> { }` 로 교체.

## Issue 2 — OffsetMappingTest (이미 #33 PR에서 해결)

PR #53에서 #33을 수정하면서 OffsetMappingTest를 올바른 "마지막 기여자" 의미론으로 재작성.
추가 작업 없음.
