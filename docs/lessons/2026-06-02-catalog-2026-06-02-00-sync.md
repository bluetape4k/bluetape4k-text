# Catalog 2026-06-02-00 동기화

## 배경

`bluetape4k-dependencies`는 issue #101 보안 의존성 train 이후
`catalog/2026-06-02-00`을 생성했다.

## 결정

기본 `bt4k` catalog ref로 `catalog/2026-06-02-00`을 사용하고, 중앙 catalog에서
공유 버전 alias를 동기화한다.

## 결과

이 저장소는 이제 공유 의존성 관리를 위해 최신 중앙 catalog ref를 사용한다.

## 검증

- `scripts/sync-shared-versions.py --workspace /Users/debop/work/bluetape4k --check --summary`
- `scripts/sync-dependabot-ignores.py --workspace /Users/debop/work/bluetape4k --check --summary`

## 향후 지침

중앙에서 관리하는 의존성을 저장소 로컬에 고정하지 않는다. 중앙 catalog ref와
동기화된 alias를 사용한다.
