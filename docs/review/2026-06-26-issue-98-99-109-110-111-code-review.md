# Issue 98/99/109/110/111 준비성 검토

## 범위

Milestone 0.2.1 준비성 작업:

- #98 Lingua detector accuracy mode와 startup tradeoff
- #99 tokenizer와 blockword 웹 서비스 safety limit
- #109 실행 가능한 Aho-Corasick search scenario
- #110 실행 가능한 Lingua detector 재사용과 혼합 언어 sample
- #111 tokenizer와 blockword web-safety sample

## 검토 초점

- 예제는 실행 가능한 application module이지만 publish 대상이 아니다.
- Public example은 기존 API만 사용한다.
- README locale pair는 example entrypoint와 safety boundary를 문서화한다.
- CI와 Nightly는 예제를 `test`와 `run`으로 모두 실행한다.
- Stable release action은 이 readiness PR 범위 밖에 남는다.

## 증거

- `./gradlew :examples:text-search-examples:test :examples:lingua-examples:test :examples:tokenizer-safety-examples:test :examples:text-search-examples:run :examples:lingua-examples:run :examples:tokenizer-safety-examples:run --no-daemon`
  - 결과: PASS
- `./gradlew :lingua:test --tests "io.bluetape4k.lingua.LanguageDetectorBuilderTest" --no-daemon`
  - 결과: PASS, 9 tests
- `actionlint .github/workflows/ci.yml .github/workflows/nightly-tests.yml`
  - 결과: PASS
- `./gradlew :examples:text-search-examples:tasks --all --no-daemon`
  - 결과: PASS, publish/sign/NMCP/Kover task 없음
- `./gradlew :examples:lingua-examples:tasks --all --no-daemon`
  - 결과: PASS, publish/sign/NMCP/Kover task 없음
- `./gradlew :examples:tokenizer-safety-examples:tasks --all --no-daemon`
  - 결과: PASS, publish/sign/NMCP/Kover task 없음
- `./gradlew build -x test --no-daemon`
  - 결과: PASS
- `./gradlew projects --no-daemon`
  - 결과: PASS, examples module 등록됨

## Reviewer 발견 사항

최종 code-reviewer pass는 처음에 다음 변경을 요청했다.

- P1: tokenizer safety sample이 예상하지 못한 processor failure를 400으로 mapping했다.
  Processor exception을 정제된 500 response로 mapping하고 tokenize/blockword exception test를 추가해 수정했다.
- P2: `:examples` parent project가 깨진 `run` task를 노출했다.
  실행 가능한 leaf example에만 `application`을 적용해 수정했다.
- P2: `examples/README.md`와 `examples/README.ko.md`가 없었다.
  이중 언어 examples index를 추가해 수정했다.
- P3: 이 review artifact에 pending marker가 남아 있었다.
  최종 finding을 여기에 기록해 수정했다.

## 릴리스 경계

이 PR은 tag 생성, publish, merge, milestone 종료, 날짜가 붙은 0.2.1 changelog section 생성을 수행하지 않는다. Stable 0.2.1 release prep은 별도 명시적 release-prep 요청이 `baseVersion`, release note, Nightly/full validation, release workflow dispatch 증거를 업데이트할 때까지 계속 막혀 있다.
