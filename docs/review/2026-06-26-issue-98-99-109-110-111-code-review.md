# Issue 98/99/109/110/111 Readiness Review

## Scope

Milestone 0.2.1 readiness work for:

- #98 Lingua detector accuracy modes and startup tradeoffs
- #99 tokenizer and blockword web-service safety limits
- #109 runnable Aho-Corasick search scenarios
- #110 runnable Lingua detector reuse and mixed-language samples
- #111 tokenizer and blockword web-safety sample

## Review Focus

- Examples are runnable application modules but remain non-published.
- Public examples use existing APIs only.
- README locale pairs document example entrypoints and safety boundaries.
- CI and Nightly run examples through both `test` and `run`.
- Stable release actions remain out of scope for this readiness PR.

## Evidence

- `./gradlew :examples:text-search-examples:test :examples:lingua-examples:test :examples:tokenizer-safety-examples:test :examples:text-search-examples:run :examples:lingua-examples:run :examples:tokenizer-safety-examples:run --no-daemon`
  - Result: PASS
- `./gradlew :lingua:test --tests "io.bluetape4k.lingua.LanguageDetectorBuilderTest" --no-daemon`
  - Result: PASS, 9 tests
- `actionlint .github/workflows/ci.yml .github/workflows/nightly-tests.yml`
  - Result: PASS
- `./gradlew :examples:text-search-examples:tasks --all --no-daemon`
  - Result: PASS, no publish/sign/NMCP/Kover tasks
- `./gradlew :examples:lingua-examples:tasks --all --no-daemon`
  - Result: PASS, no publish/sign/NMCP/Kover tasks
- `./gradlew :examples:tokenizer-safety-examples:tasks --all --no-daemon`
  - Result: PASS, no publish/sign/NMCP/Kover tasks
- `./gradlew build -x test --no-daemon`
  - Result: PASS
- `./gradlew projects --no-daemon`
  - Result: PASS, examples modules registered

## Reviewer Findings

Final code-reviewer pass initially requested changes:

- P1: tokenizer safety sample mapped unexpected processor failures to 400.
  Fixed by mapping processor exceptions to sanitized 500 responses and adding
  tokenize/blockword exception tests.
- P2: `:examples` parent project exposed a broken `run` task.
  Fixed by applying `application` only to runnable leaf examples.
- P2: `examples/README.md` and `examples/README.ko.md` were missing.
  Fixed by adding the bilingual examples index.
- P3: this review artifact still had a pending marker.
  Fixed by recording the final findings here.

## Release Boundary

This PR does not tag, publish, merge, close the milestone, or create a dated
0.2.1 changelog section. Stable 0.2.1 release prep remains blocked until a
separate explicit release-prep request updates `baseVersion`, release notes,
Nightly/full validation, and release workflow dispatch evidence.
